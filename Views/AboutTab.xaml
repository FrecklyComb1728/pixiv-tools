<UserControl x:Class="PixivTools.Views.AboutTab"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8"
                BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
                Padding="24" MaxWidth="420" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/Resources/logo.png" Width="64" Height="64" Margin="0,0,0,12"/>
                <TextBlock Text="Pixiv Tools" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <TextBlock Text="v4.0" FontSize="13" HorizontalAlignment="Center" Margin="0,3,0,20"
                          Foreground="{StaticResource Text3Brush}"/>
                <Separator Margin="0,0,0,16"/>
                <TextBlock x:Name="GithubInfo" Text="正在从 GitHub 获取信息..." FontSize="12"
                          HorizontalAlignment="Center" Foreground="{StaticResource Text2Brush}"
                          TextWrapping="Wrap" TextAlignment="Center"/>
                <Separator Margin="0,16"/>
                <Button Content="GitHub" Click="OnGitHubClick" Style="{StaticResource Btn}"
                        Width="120" Height="28" Margin="0,12,0,0" FontSize="11"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
